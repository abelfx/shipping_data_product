name: 'medical_warehouse'
version: '1.0'
config-version: 2

profile: 'medical_warehouse'  # must match profiles.yml name

source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"      # compiled models
clean-targets: ["target", "dbt_modules"]

models:
  medical_warehouse:
    staging:
      +materialized: view
    marts:
      +materialized: table
